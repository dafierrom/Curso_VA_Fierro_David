# Curso de Visión Artificial  
# Fierro David - Ingeniería en Mecatrónica
#Realizar un ejercicio capturando eventos con el mouse donde se pulse el Botón izquierdo dibuje un circulo, pulsamos el botón derecho dibuja un rectángulo

import numpy as np
import cv2 as cv

#Creamos funciones para dibujar cada figura geométrica requerida 
def draw_circle_recta(event, x, y, flags, param):
    #Círculo
    if(event == cv.EVENT_FLAG_LBUTTON):
        cv.circle(imagen, (x, y), 25, (255,0,0), 1 )
    #Rectángulo  
    elif(event == cv.EVENT_FLAG_RBUTTON):
        cv.rectangle(imagen,(x,y), (50,50),(255,0,0),1)

#Creamos salida de imagen 
imagen = np.zeros((512,512,3), np.uint8)

#Escritura de figura geométrica
cv.namedWindow('Fierro David Deber 1')
cv.setMouseCallback('Fierro David Deber 1', draw_circle_recta)


while(True):
    cv.imshow('Fierro David Deber 1',imagen)
    if (cv.waitKey(1) & 0xFF ==ord('q')):
        break
        
cv.destroyAllWindows()
